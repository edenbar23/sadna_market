# ===========================================
# DEVELOPMENT PROFILE CONFIGURATION
# ===========================================

# ===========================================
# NEON REMOTE DATABASE CONFIGURATION
# ===========================================
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-jolly-surf-a23gjojm-pooler.eu-central-1.aws.neon.tech:5432/sadna_market_db?sslmode=require}
spring.datasource.username=${DB_USERNAME:sadna_market_db_owner}
spring.datasource.password=${DB_PASSWORD:npg_Jb58mNDBoRaP}
spring.datasource.driver-class-name=org.postgresql.Driver

# Optimized connection settings for Neon
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# JPA/Hibernate Configuration for development
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# ===========================================
# EXTERNAL API CONFIGURATION (DEV)
# ===========================================
external.api.enabled=true
external.api.url=${EXTERNAL_API_URL:https://damp-lynna-wsep-1984852e.koyeb.app/}
external.api.connection.timeout.seconds=10
external.api.request.timeout.seconds=30
external.api.retry.attempts=3
external.api.retry.delay.seconds=2

# ===========================================
# SYSTEM INITIALIZATION CONFIGURATION
# ===========================================
system.init.enabled=false
system.init.reset.enabled=false
system.init.initial-state-file=src/main/resources/initial-state.txt

system.init.admin.username=u1
system.init.admin.password=Password123!
system.init.admin.email=u1@market.com
system.init.admin.first-name=System
system.init.admin.last-name=Administrator

# ===========================================
# JWT CONFIGURATION
# ===========================================
market.jwt.expiration=86400000
market.jwt.secret=${JWT_SECRET:MySecretJWTKey1234567890!@#$%^&*()}

# ===========================================
# ENCRYPTION CONFIGURATION
# ===========================================
app.encryption.key=${ENCRYPTION_KEY:MySecretEncryptionKey1234567890!}

# ===========================================
# LOGGING CONFIGURATION (DEV)
# ===========================================
logging.level.com.sadna_market=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# External API logging
logging.level.com.sadna_market.market.InfrastructureLayer.ExternalAPI=DEBUG
logging.level.com.sadna_market.market.InfrastructureLayer.Payment.ExternalPaymentAPI=DEBUG
logging.level.com.sadna_market.market.InfrastructureLayer.Supply.ExternalSupplyAPI=DEBUG

# Authentication logging
logging.level.com.sadna_market.market.InfrastructureLayer.Authentication=DEBUG

# Database connection logging
logging.level.com.zaxxer.hikari.HikariConfig=DEBUG
logging.level.com.zaxxer.hikari=INFO